# Desktop platform controllers

Optional **desktop-specific** controller overrides. Shared controllers live in `src/controllers/`. Add here when the desktop app needs different behavior than web/mobile.

# Mobile platform controllers

Optional **mobile-specific** controller overrides. Shared controllers live in `src/controllers/`. Add here for mobile-only endpoints or response shapes (e.g. lighter payloads, different pagination).

# Web platform controllers

Optional **web-specific** controller overrides. Shared controllers live in `src/controllers/`. Add here only when you need different behavior for the web client (e.g. different response shape or extra fields).
